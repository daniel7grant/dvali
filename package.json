{
    "name": "dvali",
    "version": "0.2.5",
    "description": "Simple, extensible, functional validation library written in TypeScript",
    "main": "./dist/index.js",
    "module": "./es/index.js",
    "types": "./dist/index.d.ts",
    "exports": {
        "require": "./dist/index.js",
        "import": "./es/index.js",
        "default": "./es/index.js"
    },
    "scripts": {
        "build": "tsc -p tsconfig.json && tsc -p tsconfig.es.json",
        "prepare": "npm run build",
        "size": "npm run build && size-limit",
        "test": "nyc ava -v",
        "test:watch": "ava -v --watch"
    },
    "keywords": [
        "validation",
        "sanitization",
        "transform"
    ],
    "sideEffects": false,
    "files": [
        "dist/**/*",
        "es/**/*"
    ],
    "author": "Daniel Grant",
    "license": "MIT",
    "homepage": "https://github.com/daniel7grant/dvali",
    "repository": {
        "type": "git",
        "url": "https://github.com/daniel7grant/dvali"
    },
    "devDependencies": {
        "@ava/typescript": "^3.0.1",
        "@size-limit/preset-small-lib": "^7.0.4",
        "ava": "^4.0.0-rc.1",
        "doctoc": "^2.1.0",
        "nyc": "^15.1.0",
        "prettier": "^2.3.2",
        "size-limit": "^7.0.4",
        "ts-node": "^10.1.0",
        "typescript": "^4.5.4"
    },
    "ava": {
        "typescript": {
            "rewritePaths": {
                "src/": "dist/"
            },
            "compile": false
        },
        "require": [
            "ts-node/register"
        ]
    },
    "nyc": {
        "all": true,
        "include": [
            "src/**"
        ],
        "reporter": [
            "html"
        ],
        "report-dir": ".coverage"
    },
    "size-limit": [
        {
            "path": "es/validate.js",
            "limit": "1 kB",
            "import": "validate"
        },
        {
            "path": "es/index.js",
            "limit": "5 kB",
            "import": "{ Failure, Ignore, Success, validate, isEmail, toString, insensitiveEquals, length, minLength, isUrl, maxLength, betweenLength, isString, validateRegex, oneOf, optional, validateCondition, arrayOf, bail, tryDate, after, inRange, before, toDate, isDate, falsey, truthy, isBool, toBool, tryBool, lt, tryInt, lte, gte, tryNumber, gt, closeTo, toNumber, isInt, isNumber, toInt, between, equals, ceil, clamp, floor, round, lower, normalize, trim, truncate, upper }"
        }
    ]
}
